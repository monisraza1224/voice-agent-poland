services:
  - type: web
    name: voice-agent-poland
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: TELNYX_API_KEY  
        sync: false
